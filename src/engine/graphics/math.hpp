#ifndef BLOSSOM_ENGINE_GRAPHICS_MATH_HEADER
#define BLOSSOM_ENGINE_GRAPHICS_MATH_HEADER

#define clamp(x, min, max) ((x) < (min) ? (min) : ((x) > (max) ? (max) : (x)))

#endif // !BLOSSOM_ENGINE_GRAPHICS_MATH_HEADER